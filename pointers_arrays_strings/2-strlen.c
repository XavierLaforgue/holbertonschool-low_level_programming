/**
 * _strlen - extracts the length of a string
 * @s: character string
 *
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int size;

	for (size = 0; *(s + size) != '\0'; size++)
	{
	}
	return (size);
}
